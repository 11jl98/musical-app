import React from 'react';
import PlaylistView from '../views/playlist.view';

export default function PlaylistController({ route }: any) {
    return (

        (
            <PlaylistView route={route}/>
        )
    );
}